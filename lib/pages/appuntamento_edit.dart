import 'package:flutter/material.dart';

class AppuntamentoEdit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Aggiungi appuntamento'),
      ),
      body: Container(
        child: Text('Text'),
      ),
    );
  }
}
