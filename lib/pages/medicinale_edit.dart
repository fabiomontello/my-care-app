import 'package:flutter/material.dart';

class MedicinaleEdit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Aggiungi medicinale'),
      ),
      body: Container(
        child: Text('Text'),
      ),
    );
  }
}
